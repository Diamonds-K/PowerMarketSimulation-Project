# 项目级开发约束（供代码生成与后续开发使用）

本目录内所有代码开发，包括人工修改、AI 生成、重构和评审，必须遵守以下规则：

1. **先读约束文档**：任何代码改动前，先阅读 `ARCHITECTURE.md` 和 `DESIGN.md`。
2. **架构优先**：代码必须放入 `ARCHITECTURE.md` 第 3 节定义的目录结构中，不允许新增顶层分层或绕过统一接口。
3. **分层纪律**：
   - UI 只能调用 `TradingCenter`；
   - 算法层（`Model/Validation/Clearing/Settlement`）禁止包含 Qt 头文件；
   - `Data` 层禁止调用业务逻辑；
   - `Model` 只保存数据。
4. **数学一致**：双指针撮合、KKT/λ 二分、价格规则、边界规则、结算单位必须与 `DESIGN.md` 一致。
5. **测试同步**：新增或修改算法时，必须在 `Test` 对应目录补充/更新测试。
6. **不改变项目方向**：禁止把项目改成非日前市场模型、多节点网络模型或引入新的定价机制。
